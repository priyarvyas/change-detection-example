import { CalculateCodePipe } from './calculate-code.pipe';

describe('CalculateCodePipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateCodePipe();
    expect(pipe).toBeTruthy();
  });
});
